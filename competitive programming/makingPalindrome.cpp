#include <bits/stdc++.h>
using namespace std;





int main(){
    int n; cin>>n;
    vector<string> strings;
    vector<int> costs;
    while(n--){
        string s; int x;
        strings.push_back(s); costs.push_back(x);
    }
    for (int i=0;i<strings.size();i++){
        
    }

}